#######################################################
## This is a code entry in the error correction zoo. ##
##       https://github.com/errorcorrectionzoo       ##
#######################################################

code_id: construction_a
physical: reals
logical: reals

name: 'Construction-\(A\) code'
introduced: '\cite{doi:10.4153/CJM-1967-017-0}'

description: |
  Sphere packing constructed from a binary \((n,K)\) code using Construction \(A\) \cite{doi:10.1007/978-1-4757-6568-7}.
  Each binary codeword \(c\) of the code is mapped to an infinite set of points \(x\) such that \(x = c\) modulo two.
  If the underlying binary code is linear, then the resulting set of points forms a lattice.

relations:
  parents:
    - code_id: analog
  cousins:
    - code_id: bits_into_bits
      detail: 'Each binary code yields a sphere packing under Construction \(A\).'
    - code_id: binary_linear
      detail: 'Every binary linear code yields a lattice code under Construction \(A\).'


# Begin Entry Meta Information
_meta:
  # Change log - most recent first
  changelog:
    - user_id: VictorVAlbert
      date: '2022-11-08'

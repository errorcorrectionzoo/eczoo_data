#######################################################
## This is a code entry in the error correction zoo. ##
##       https://github.com/errorcorrectionzoo       ##
#######################################################

code_id: group_classical
physical: groups
#logical: groups

name: 'Group-based code'

description: 'Encodes \(K\) states (codewords) in \(n\) coordinates labeled by elements of a group \(G\). The number of codewords may be infinite for infinite groups, so various restricted versions have to be constructed in practice.'

relations:
  parents:
    - code_id: ecc
      detail: ''
  cousins:
    - code_id: bits_into_bits
      detail: 'Group-based codes whose alphabet is based on the field \(GF(2)\), taken to be an abelian group under addition, are binary codes.'
    - code_id: q-ary_digits_into_q-ary_digits
      detail: 'Group-based codes whose alphabet is based on the field \(GF(q)\), taken to be an abelian group under addition, are \(q\)-ary codes.'
    - code_id: analog
      detail: 'Group-based codes whose alphabet is based on the reals \(\mathbb{R}\), taken to be an abelian group under addition, are analog codes.'
    - code_id: rings_into_rings
      detail: 'Group-based codes whose alphabet is based on the ring \(R\), taken to be an abelian group under addition, are codes over \(R\).'
    - code_id: group_quantum


# Begin Entry Meta Information
_meta:
  # Change log - most recent first
  changelog:
    - user_id: VictorVAlbert
      date: '2022-03-24'

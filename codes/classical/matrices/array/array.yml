#######################################################
## This is a code entry in the error correction zoo. ##
##       https://github.com/errorcorrectionzoo       ##
#######################################################


code_id: array
physical: matrices

name: 'Array code'

alternative_names:
  - 'RAID code'
  - 'Disk array code'


description: |
  Matrix code over \(\mathbb{F}_q\) designed for use in distributed storage, with the first such application being a RAID-type array of hard-drives such that information is protected against erasure of one or more hard drives.
  Each column of a matrix codeword is typically treated as a single codeword coordinate via subpacketization (defined below) and represents a large data block.
  Array codes are denoted by \((n,k,m)\), where \(n\) is the number of nodes, \(k\) is the number of nodes needed to recover the data, and \(m\) is the column dimension of each codeword a.k.a. the subpacketization level.

protection: |
  Since they are designed to protect against corruption of nodes in a distributed storage network, many array codes are analyzed in terms of their erasure capabilities.

  An analogue of the Hamming metric for array codes comes from counting the number of column positions in which two codewords differ.
  Equivalently, one may treat each length-\(m\) column as a single symbol from an alphabet of size \(q^m\); this interpretation underlies the subpacketization viewpoint.
  Array codes are typically \(\mathbb{F}_q\)-linear as matrix codes. In some constructions, after choosing an identification of columns with symbols from an extension field \(\mathbb{F}_{q^m}\), one also obtains an \(\mathbb{F}_{q^m}\)-linear code.

  There are other notions of distance for array codes, including the rank metric and its generalization the sum-rank metric.

notes:
  - 'See \cite{preset:HPArray}\cite[Ch. 14]{doi:10.1002/0471792748}\cite{preset:HPArray} for introductions.'


relations:
  parents:
    - code_id: matrices_into_matrices
    - code_id: distributed_storage
  cousins:
    - code_id: reed_solomon
      detail: 'RS codes over \(q=2^m\) are used in RAID 6 \cite{preset:Anvin07,preset:DDFspec09}; see \cite{preset:HPArray}.'


# Begin Entry Meta Information
_meta:
  # Change log - most recent first
  changelog:
    - user_id: VictorVAlbert
      date: '2023-05-04'

#######################################################
## This is a code entry in the error correction zoo. ##
##       https://github.com/errorcorrectionzoo       ##
#######################################################

code_id: homological_rotor
physical: groups
logical: groups

name: 'Homological rotor code'
introduced: '\cite{arXiv:2303.13723}'

description: |
  A homological quantum rotor code is stabilized by a set of X-type and Z-type generalized Pauli operators for rotors.
  It is an extension of \hyperref[code:css]{CSS} codes to quatum rotors.

  \subsection{Stabilizer group}
  The stabilizer group is defined using two integer matrices \(H_X\in\mathbb{Z}^{r_X\times n}\) and \(H_Z\in\mathbb{Z}^{r_Z\times n}\) which are such that
  \begin{align}
        H_XH_Z^T = 0.\label{eq:commutation}
  \end{align}
  The stabilizer is then defined as
  \begin{align}
\mathcal{S} = \left\langle X(\boldsymbol{s}H_X)Z(\boldsymbol{\varphi}H_Z\middle\vert\forall\boldsymbol{s}\in\mathbb{Z}^{r_X},\forall\boldsymbol{\varphi}\in[0,2\pi)^{r_Z}\right\rangle.\label{eq:stabilizer}
  \end{align}
  The condition \eqref{eq:commutation} ensures that \(\mathcal{S}\) has a common +1 eigenspace.

  \subsection{Link with homology of a chain complex}
  As with \hyperref[code:css]{CSS} codes, there is a natural connection to a length-3 integer chain complexes
  \begin{align}
  \mathcal{C}:~ \mathbb{Z}^{r_X} \xrightarrow{H_X} \mathbb{Z}^n \xrightarrow{H_Z^T} \mathbb{Z}^{r_Z}
  \end{align}
  whose middle homology group describes the logical \(X\) operators fo the code.
  The logical \(Z\) operators are defined by the middle cohomology group where the cohomology is taken with phase coefficients, \mathbb{T} = \mathbb{R}/2\pi\mathbb{Z}\),
  \begin{align}
  \mathcal{C}^*:~ \mathbb{T}^{r_X} \xleftarrow{H_X^T} \mathbb{T}^n \xleftarrow{H_Z} \mathbb{T}^{r_Z}.
  \end{align}

  \subsection{Logical subspace}
  The logical subspace can contain logical rotors as well as logical qudits.
  The former correspond to the so called free part of the homology group while the latter correspond to the torsion part
  \begin{align}
  H_1(\mathcal{C},\mathbb{Z}) = \mathbb{Z}^{k^\prime}\oplus\mathbb{Z}_{d_1}\oplus\cdots\oplus\mathbb{Z}_{d_{k^{\prime\prime}}}.
  \end{align}

protection: 'Protects against generalized Pauli noise.'

features:
  transversal_gates: 'All generalized Pauli gates are realized transversally.'

notes:
  - 'A sage notebook to look at small examples can be found in \url{https://github.com/cianibegood/quantum-rotor-codes}.'

relations:
  parents:
    - code_id: code_rotor
    - code_id: code_css
  children:
    - code_id: code_rotor_3_1_2
      detail: 'Taking \(H_X=\begin{pmatrix}-3 & 1 & 2\end{pmatrix}\) and \(H_Z=\begin{pmatrix}4&6&3\end{pmatrix}\) yields the three-rotor code.'
  cousins:
    - code_id: code_group_gkp
    - code_id: code_rotor_gkp
    - code_id: code_generalized_homological_product

_meta:
  # Change log - most recent first
  changelog:
    - user_id: ChristopheVuillot
      date: '2023-03-28'

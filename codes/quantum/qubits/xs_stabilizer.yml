#######################################################
## This is a code entry in the error correction zoo. ##
##       https://github.com/errorcorrectionzoo       ##
#######################################################

code_id: xs_stabilizer
physical: qubits
logical: qubits

name: 'XS stabilizer code'
introduced: '\cite{arXiv:1404.5327}'

description: |
  A type of stabilizer code where stabilizer generators are elements of the group \( \{\alpha I, X, S\}^{\otimes n} \). Here, \( \alpha \) is an \(8\)th root of unity and \( S = \text{diag} ( 1, \alpha^2) \). The codespace is a joint \(+1\) eigenspace of a set of stabilizer generators, which need not commute to define a valid codespace.

relations:
  parents:
    - code_id: xp_stabilizer
      detail: 'The XP stabilizer formalism reduces to the XS formalism at \(N=4\).'
  cousins:
    - code_id: topological_abelian
      detail: 'Twisted quantum double models for the groups \(\mathbb{Z}_2^k\) can be realized as XS stabilizer codes.'

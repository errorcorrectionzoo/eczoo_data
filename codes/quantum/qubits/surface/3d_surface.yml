#######################################################
## This is a code entry in the error correction zoo. ##
##       https://github.com/errorcorrectionzoo       ##
#######################################################

code_id: 3d_surface
physical: qubits
logical: qubits

name: 'Solid code'
introduced: \cite{arxiv:1406.4227}

description: 'A variant of Kitaev's surface code on a 3D lattice.'

#protection: As a stabilizer code, \([[n=O(d^2), k=O(1), d]]\).

#features:
#  threshold:
#  - '\(6.32(3)\%\) for infinite \(Z\) bias, and thresholds of \(\sim 5\%\) for \(Z\) bias around \(\eta = 100\) using a variant of the minimum-weight perfect matching decoder \cite{arXiv:1907.02554}.'
#  code_capacity_threshold:
#  - '\(50\%\) at infinite \(Z\) bias with maximum-likelihood decoder \cite{arxiv:1812.08186}.'
#  - '\(18.7\%\) for standard depolarising noise with maximum-likelihood decoder \cite{arxiv:1812.08186}.'

relations:
  parents:
    - code_id: higher_dimensional_surface
  cousins:
    - code_id: color
      detail: 'Color code is equivalent to surface code in several ways \cite{arxiv:1503.02065,arXiv:1804.00866}. For example, the color code on a \(D\)-dimensional closed manifold is equivalent to multiple decoupled copies of the \(D-1\)-dimensional surface code.'

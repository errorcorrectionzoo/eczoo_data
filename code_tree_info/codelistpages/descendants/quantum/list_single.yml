
list_id: 'single'

title: |
  Quantum single-error detecting or correcting stabilizer codes

intro: |
  Here is a list of stabilizer codes defined on \(n\) identical subsystems (e.g., qubits, modular qudits, or Galois qudits) that detect or correct an error on at most one subsystem.


display:
  style: table
  options:
    cssclass: xtra-xtra-stretch
    columns:
      - property: name
        title: Code
        link_to_code: true
        cssclass: 'top left'

      - property: description
        title: Description
        cssclass: 'textit'
        first_paragraph_only: true

sort:
  by: name

codes:
  select:
    - descendant_of: small_distance
